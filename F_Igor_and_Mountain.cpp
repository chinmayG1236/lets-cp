#include<bits/stdc++.h>
using namespace std;
using ll=long long;

void solve(){
    ll n,m,d;cin>>n>>m>>d;
    vector<string>mat;
    for(int i=0;i<n;i++){
        string s; cin>>s;
        mat.push_back(s);
    }
    
}
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int t;cin>>t;
    while(t--)solve();
}