#include<bits/stdc++.h>
using namespace std;
using ll=long long;

void solve(){
    ll n;cin>>n;
    ll s= cbrtl(n);
    
    for(ll x=(int)s -1;3*x*x + 3*x + 1<=n;x++){
        double y= cbrtl();
    }
}
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    solve();
}